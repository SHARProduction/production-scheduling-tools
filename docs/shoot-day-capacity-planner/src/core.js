export const example={start:'08:00',end:'18:00',setups:[{id:'s1',durationMinutes:240,bufferMinutes:30},{id:'s2',durationMinutes:300,moveMinutes:30}]};
const mins=s=>{const m=/^(\d{2}):(\d{2})$/.exec(s??'');return m&&+m[1]<24&&+m[2]<60?+m[1]*60+ +m[2]:null};
export function evaluate(x){const errors=[],a=mins(x?.start),b=mins(x?.end);if(a===null||b===null)return{valid:false,errors:[{code:'WINDOW'}],timeline:[]};let capacity=b-a;if(capacity<=0)capacity+=1440;if(!Array.isArray(x.setups))return{valid:false,errors:[{code:'SETUPS'}],timeline:[]};let cursor=0,unknown=false;const timeline=x.setups.map((s,i)=>{if(s.durationMinutes==null){unknown=true;errors.push({code:'UNKNOWN_DURATION',index:i});return{id:s.id,startMinute:null,endMinute:null}}for(const k of['durationMinutes','bufferMinutes','moveMinutes'])if(s[k]!=null&&(!Number.isFinite(s[k])||s[k]<0))errors.push({code:'DURATION',index:i,field:k});const start=cursor;cursor+=(s.durationMinutes??0)+(s.bufferMinutes??0)+(s.moveMinutes??0);return{id:s.id,startMinute:start,endMinute:cursor}});return{valid:errors.length===0,errors,capacityMinutes:capacity,requiredMinutes:unknown?null:cursor,unusedMinutes:unknown?null:capacity-cursor,overCapacity:unknown?null:cursor>capacity,timeline}}
